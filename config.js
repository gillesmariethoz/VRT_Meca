// GitHub Pages frontend; the shared API is hosted separately. Local previews use their own database.
if (location.hostname === 'gillesmariethoz.github.io') {
  window.VRT_API = 'https://vrt-mec-atelier.foamy-cedar-3664.chatgpt.site/api';
}
